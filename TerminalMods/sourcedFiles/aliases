alias whereami="hostnamectl | grep 'Static hostname' | cut -d ':' -f 2"

if [[ "$OSTYPE" == "linux-gnu" ]]; then
  alias rsync="rsync --info=progress2"
  alias dd="dd status=progress"
fi

#alias rm="echo 'use trash'"
alias ap="ansible-playbook"
