###My Modfications Start
##custom script dir start
export RESOURCEDIR="$HOME/resources"
export SOURCEDPROGDIR="$RESOURCEDIR/TerminalMods/sourcedFiles"
##custom script dir end

##phil's terminal mod start

if [[ -f "$SOURCEDPROGDIR/philTerminalMod" ]];then 
	source "$SOURCEDPROGDIR/philTerminalMod"
fi
##phil's terminal mod end

##farsos directory movement start
up (){
if [[ -f "$SOURCEDPROGDIR/up" ]];then 
	source "$SOURCEDPROGDIR/up"
fi
}
##farsos directory movement end
###My Modfications End
